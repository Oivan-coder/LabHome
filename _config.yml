# Jekyll конфигурация для GitHub Pages
include: [".html"]
permalink: pretty

# Настройки для работы без .html в URL
plugins:
  - jekyll-redirect-from

# Редиректы для старых URL с .html
defaults:
  - scope:
      path: ""
    values:
      permalink: pretty
