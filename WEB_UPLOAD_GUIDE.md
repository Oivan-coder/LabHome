# 📤 Загрузка через веб-интерфейс GitHub

## ⚠️ ВАЖНО: Проблемы с веб-загрузкой

### Проблема 1: Скрытые файлы (`.nojekyll`)
Файлы, начинающиеся с точки (`.nojekyll`), могут **НЕ ЗАГРУЗИТЬСЯ** через веб-интерфейс GitHub, потому что:
- Они скрыты в файловой системе
- Веб-интерфейс может их не показать
- **БЕЗ `.nojekyll` главная страница НЕ БУДЕТ РАБОТАТЬ!**

### Проблема 2: Большие файлы
- Файлы больше 100MB не загрузятся через веб-интерфейс
- Видео файлы нужно исключить (уже сделано через .gitignore)

---

## ✅ Как загрузить через веб-интерфейс (если все же хотите)

### Вариант 1: Drag & Drop (для видимых файлов)

1. Откройте ваш репозиторий на GitHub
2. Нажмите "Add file" → "Upload files"
3. Перетащите папку или файлы
4. **НО:** `.nojekyll` может не загрузиться!

### Вариант 2: Создать `.nojekyll` прямо на GitHub

1. В репозитории нажмите "Add file" → "Create new file"
2. Введите имя файла: `.nojekyll` (с точкой в начале!)
3. Оставьте файл **ПУСТЫМ** (не пишите ничего)
4. Нажмите "Commit new file"

---

## 🎯 РЕКОМЕНДАЦИЯ: Использовать Git через терминал

Это самый надежный способ, который гарантированно загрузит все файлы, включая скрытые.

### Быстрые команды:

```bash
cd /Users/ivangolcev/Desktop/conferenc_nov-main

# Добавьте все файлы (включая .nojekyll)
git add .

# Коммит
git commit -m "Update files and add .nojekyll"

# Загрузите на GitHub
git push
```

**Преимущества:**
- ✅ Загрузит ВСЕ файлы, включая скрытые
- ✅ Обновит измененные файлы
- ✅ Добавит новые файлы
- ✅ Работает с любыми размерами файлов (кроме тех, что в .gitignore)

---

## 📋 Что произойдет при загрузке через веб-интерфейс:

1. **Измененные файлы** - будут обновлены (если загрузить заново)
2. **Новые файлы** - будут добавлены
3. **Скрытые файлы** (`.nojekyll`) - могут НЕ загрузиться ⚠️

---

## ⚡ Быстрое решение:

**Самый простой способ - использовать терминал:**

```bash
cd /Users/ivangolcev/Desktop/conferenc_nov-main
git add .
git commit -m "Update all files"
git push
```

Это займет 30 секунд и гарантированно загрузит все, включая `.nojekyll`!
