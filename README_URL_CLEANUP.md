# Инструкция по удалению .html из URL

## ✅ Что уже сделано:

1. **Создан скрипт `js/url-cleaner.js`** - автоматически убирает `.html` из URL в браузере
2. **Обновлен `_config.yml`** - настроен Jekyll для работы с чистыми URL
3. **Обновлены основные страницы:**
   - ✅ `index.html` - все ссылки обновлены
   - ✅ `about.html` - все ссылки обновлены
   - ✅ `includes/header.html` - все ссылки обновлены
   - ✅ `conf_nov2025.html` - добавлен скрипт

## 📝 Что нужно сделать для остальных страниц:

Для каждой страницы (`conf_sen2025.html`, `conf_mart2026.html`, `registration.html`, `privacy.html`, `ask.html`) нужно:

1. **Добавить скрипт перед другими скриптами:**
```html
<script src="js/url-cleaner.js"></script>
```

2. **Обновить все ссылки:**
   - `index.html` → `/`
   - `about.html` → `/about`
   - `conf_nov2025.html` → `/conf_nov2025`
   - `conf_sen2025.html` → `/conf_sen2025`
   - `conf_mart2026.html` → `/conf_mart2026`
   - `registration.html` → `/registration`
   - `privacy.html` → `/privacy`

## 🔧 Как это работает:

1. **JavaScript (`url-cleaner.js`)** - автоматически убирает `.html` из URL при загрузке страницы
2. **Jekyll (`_config.yml`)** - на GitHub Pages обрабатывает URL без расширений
3. **Обновленные ссылки** - все внутренние ссылки теперь без `.html`

## ⚠️ Важно:

- Старые URL с `.html` все еще будут работать (GitHub Pages автоматически их обрабатывает)
- Новые URL без `.html` будут работать через скрипт
- После обновления всех страниц, старые URL будут автоматически перенаправляться на новые

## 🚀 После обновления:

1. Закоммитьте изменения в Git
2. Запушьте на GitHub
3. GitHub Pages автоматически обработает изменения
4. Проверьте работу на сайте rclsmo.ru
